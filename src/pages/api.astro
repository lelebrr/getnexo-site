---
import Layout from '../layouts/Layout.astro';
---

<Layout title="API GetNexo">
  <section class="container">
    <h1>API GetNexo</h1>
    <p>Documentação da Evolution API integrada com WhatsApp oficial.</p>

    <div class="endpoint">
      <h2>Status da API</h2>
      <code>GET https://api.getnexo.com.br/status</code>
      <pre>
{
  "status": 200,
  "message": "Welcome to the Evolution API, it is working!",
  "version": "2.2.2",
  "clientName": "evolution_exchange"
}
      </pre>
    </div>

    <div class="swagger-mock">
      <h2>Swagger Mock (exemplo)</h2>
      <iframe src="https://petstore.swagger.io" width="100%" height="600px" title="Swagger UI Mock"></iframe>
      <p>Em produção, será substituído pelo Swagger real da Evolution API.</p>
    </div>

    <p><a href="/">← Voltar ao início</a></p>
  </section>
</Layout>

<style>
  .container { max-width: 1100px; margin: 6rem auto; padding: 0 5%; }
  h1 { font-size: 3rem; background: linear-gradient(90deg, #00d4ff, #00ff9d); -webkit-background-clip: text; color: transparent; }
  .endpoint { background: rgba(30, 41, 59, 0.6); backdrop-filter: blur(10px); border: 1px solid rgba(0,212,255,0.15); border-radius: 16px; padding: 2rem; margin: 2rem 0; }
  code { background: #1e293b; padding: 0.5rem 1rem; border-radius: 8px; font-family: 'Consolas', monospace; }
  pre { background: #0f172a; padding: 1.5rem; border-radius: 12px; overflow-x: auto; }
  .swagger-mock iframe { border: none; border-radius: 12px; box-shadow: 0 10px 30px rgba(0,0,0,0.3); }
</style>

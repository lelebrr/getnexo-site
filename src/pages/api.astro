---
import Layout from '../layouts/Layout.astro';
---

<Layout title="API GetNexo – Assistente de Vendas WhatsApp">
  <section class="container">
    <h1>Documentação da API GetNexo</h1>
    <p>API baseada na Evolution API (WhatsApp oficial) – integração completa com n8n e IA.</p>

    <div class="endpoint">
      <h2>Status da API</h2>
      <code>GET https://api.getnexo.com.br/status</code>
      <pre>
{
  "status": 200,
  "message": "Welcome to the Evolution API, it is working!",
  "version": "2.2.2"
}
      </pre>
    </div>

    <div class="endpoint">
      <h2>Enviar Mensagem</h2>
      <code>POST https://api.getnexo.com.br/send-message</code>
      <pre>
{
  "number": "5511999999999",
  "message": "Oi, quer fechar uma venda hoje?"
}
      </pre>
    </div>

    <p><a href="https://api.getnexo.com.br/manager" target="_blank">Acessar Manager da Evolution API</a></p>
    <p><a href="/">← Voltar ao site</a></p>
  </section>
</Layout>

<style>
  .container { max-width: 1100px; margin: 6rem auto; padding: 0 5%; }
  h1 { font-size: 3rem; background: linear-gradient(90deg, var(--primary), var(--success)); -webkit-background-clip: text; color: transparent; }
  .endpoint { background: rgba(30,41,59,0.7); backdrop-filter: blur(10px); border: 1px solid rgba(0,212,255,0.2); border-radius: 16px; padding: 2rem; margin: 2rem 0; }
  code { background: #1e293b; padding: 0.5rem 1rem; border-radius: 8px; font-family: 'Consolas', monospace; display: block; margin: 1rem 0; }
  pre { background: #0f172a; padding: 1.5rem; border-radius: 12px; overflow-x: auto; }
</style>

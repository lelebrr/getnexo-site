---
import Layout from '../layouts/Layout.astro';
---

<Layout title="GetNexo – Venda no WhatsApp 24h">
  <header style="background:#0a0e17; padding:4rem; text-align:center; min-height:80vh; display:flex; flex-direction:column; justify-content:center; align-items:center;">
    <h1 class="glow" style="font-size:6rem; color:#00d4ff; font-weight:800; margin:0;">GetNexo</h1>
    <p style="font-size:2rem; color:#e5e7eb; margin:2rem 0; font-weight:300;">
      A única plataforma que vende sozinha.<br>
      <strong style="color:#00ff9d;">Sem mensalidade. Sem truque.</strong>
    </p>
    <a href="#" id="instalar-btn" class="btn-cta" style="font-size:1.6rem; padding:1.2rem 3.5rem; text-decoration:none; color:black; background:linear-gradient(90deg,#00d4ff,#00ff9d); border-radius:50px; font-weight:800; box-shadow:0 0 20px rgba(0,212,255,0.4); display:inline-block; transition:transform 0.2s;">
        Começar agora
    </a>
  </header>

  <div id="modal-instal" style="display:none; position:fixed; inset:0; background:rgba(0,0,0,0.85); z-index:9999; align-items:center; justify-content:center;">
      <div style="background:#111827; border-radius:16px; padding:3rem; width:90%; max-width:600px; color:#e5e7eb; border:1px solid #333; box-shadow:0 0 50px rgba(0,212,255,0.2);">
        <h3 style="color:#00ff9d; text-align:center; font-size:2rem; margin-bottom:1rem;">Instale no seu site</h3>
        <p style="text-align:center; margin-bottom:1.5rem; color:#94a3b8;">Copie esse snippet e cole antes do <code style="color:#00d4ff">&lt;/body&gt;</code> do seu HTML.</p>
        
        <div style="background:#0a0e17; padding:1.5rem; border-radius:8px; border:1px solid #333; margin-bottom:1.5rem; overflow-x:auto;">
            <code style="font-family:monospace; color:#00tile; white-space:pre; font-size:0.9rem; color:#a5b4fc;">&lt;script src="https://cdn.getnexo.com.br/widget.js" data-bot="seu_id" data-theme="neon"&gt;&lt;/script&gt;</code>
        </div>
        
        <p style="text-align:center; font-size:0.9rem; color:#64748b;"><small>Substitua <strong>seu_id</strong> pelo ID do seu bot depois de criar.</small></p>
        
        <div style="text-align:center; gap:2rem; display:flex; justify-content:center; margin-top:2rem; align-items:center;">
          <a href="/dashboard" style="color:#00d4ff; text-decoration:none; font-weight:bold;">Criar meu bot →</a>
          <button id="fechar-modal-btn" style="background:#00ff9d; color:black; padding:0.8rem 2rem; border:none; border-radius:50px; font-weight:bold; cursor:pointer;">Fechar</button>
        </div>
      </div>
  </div>

  <script is:inline>
      const btn = document.getElementById('instalar-btn');
      const modal = document.getElementById('modal-instal');
      const closeBtn = document.getElementById('fechar-modal-btn');

      if(btn && modal) {
          btn.onclick = (e) => {
              e.preventDefault();
              modal.style.display = 'flex';
          };
      }
      
      if(closeBtn && modal) {
          closeBtn.onclick = () => {
              modal.style.display = 'none';
          };
      }
      
      // Close on outside click
      window.onclick = (event) => {
          if (event.target == modal) {
              modal.style.display = "none";
          }
      }
  </script>

  <section style="padding:5rem 15%; background:linear-gradient(to bottom, #0a0e17, #111827);">
    <div style="display:grid; grid-template-columns:repeat(auto-fit,minmax(280px,1fr)); gap:2rem;">
      <div class="card">
        <h3 style="color:white; font-size:1.5rem; margin-bottom:0.5rem;">📢 Envie pra milhares</h3>
        <p style="color:#94a3b8;">Campanhas inteligentes. Abre, clica, vende.</p>
      </div>
      <div class="card">
        <h3 style="color:white; font-size:1.5rem; margin-bottom:0.5rem;">🤖 IA que fecha</h3>
        <p style="color:#94a3b8;">Responde, qualifica, upsell. 24h sozinha.</p>
      </div>
      <div class="card">
        <h3 style="color:white; font-size:1.5rem; margin-bottom:0.5rem;">🛒 Loja no WhatsApp</h3>
        <p style="color:#94a3b8;">Catálogo. Carrinho. PIX na hora.</p>
      </div>
      <div class="card">
        <h3 style="color:white; font-size:1.5rem; margin-bottom:0.5rem;">💼 CRM simples</h3>
        <p style="color:#94a3b8;">Lead → qualificado → fechado. Arraste e pronto.</p>
      </div>
      <div class="card">
        <h3 style="color:white; font-size:1.5rem; margin-bottom:0.5rem;">📊 Veja o lucro</h3>
        <p style="color:#94a3b8;">Vendas, taxa de resposta, tudo num olhar.</p>
      </div>
      <div class="card">
        <h3 style="color:white; font-size:1.5rem; margin-bottom:0.5rem;">👥 Revenda e ganhe</h3>
        <p style="color:#94a3b8;">Indique. Instale. Compre. Ganhe R$500 cada.</p>
      </div>
    </div>
  </section>

  <footer style="padding:3rem; text-align:center; background:#000; color:#666; font-size:0.9rem;">
    GetNexo • Feito no Brasil • 2026 <br/>
    <a href="#" style="color:#00d4ff; text-decoration:none; margin-top:10px; display:inline-block;">Suporte</a>
  </footer>
</Layout>

<style>
  .card {
    background:rgba(30,41,59,0.8);
    backdrop-filter:blur(12px);
    border:1px solid rgba(0,212,255,0.2);
    border-radius:16px;
    padding:2rem;
    text-align:center;
    transition:0.3s all ease;
    cursor: default;
  }
  .card:hover { 
      transform:translateY(-10px); 
      border-color:#00ff9d; 
      box-shadow: 0 10px 30px rgba(0,0,0,0.5);
  }
  .glow { text-shadow: 0 0 40px rgba(0,212,255,0.4); }
  .btn-cta:hover { transform: scale(1.05); }
</style>

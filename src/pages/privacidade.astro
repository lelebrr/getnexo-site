---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Privacidade Zero-Knowledge — GetNexo">
    <div class="privacy-container">
        <header>
            <h1>MANIFESTO <span class="text-neon">ZERO-KNOWLEDGE</span></h1>
            <p class="updated">Nós não queremos seus dados.</p>
        </header>

        <article class="glass-card">
            <p class="lead">
                Diferente das Big Techs, nossa arquitetura é descentralizada. 
                O software roda na <strong>sua máquina</strong>, no <strong>seu Docker</strong>.
            </p>

            <div class="grid">
                <div class="point">
                    <h3>🔒 Criptografia AES-256</h3>
                    <p>Do Cloudflare Tunnel até o seu banco Postgres local. Nada trafega em texto claro.</p>
                </div>

                <div class="point">
                    <h3>⛔ Sem Telemetria</h3>
                    <p>O código é aberto em <code>/scripts</code>. Nenhuma informação sai sem você saber.</p>
                </div>

                <div class="point">
                    <h3>📜 Logs Locais</h3>
                    <p>Auditoria completa no seu disco. Nós não temos acesso a nada.</p>
                </div>

                <div class="point">
                    <h3>💥 Kill Switch</h3>
                    <p>Apague tudo com um comando. Soberania digital total.</p>
                </div>
            </div>

            <div class="legal">
                <h3>Termos de Uso</h3>
                <p>O GetNexo é uma ferramenta ("tooling"). Você é responsável pelo uso ético do assistente.</p>
            </div>

            <a href="/" class="btn-back">← Voltar para a Base</a>
        </article>
    </div>
</Layout>

<style>
    .privacy-container { padding: 6rem 5%; max-width: 900px; margin: 0 auto; }
    header { text-align: center; margin-bottom: 4rem; }
    h1 { font-size: 3.5rem; margin-bottom: 1rem; }
    .text-neon { color: var(--primary); text-shadow: 0 0 20px rgba(0,212,255,0.3); }
    .updated { color: var(--text); font-family: 'JetBrains Mono', monospace; }

    .glass-card {
        background: rgba(30,41,59,0.6); backdrop-filter: blur(10px);
        border: 1px solid rgba(255,255,255,0.1); border-radius: 20px;
        padding: 3rem;
    }

    .lead { font-size: 1.4rem; color: var(--light); margin-bottom: 3rem; text-align: center; }
    strong { color: var(--success); }

    .grid { display: grid; grid-template-columns: 1fr 1fr; gap: 2rem; margin-bottom: 3rem; }
    @media (max-width: 768px) { .grid { grid-template-columns: 1fr; } }

    .point h3 { color: var(--primary); margin-bottom: 0.5rem; font-size: 1.3rem; }
    .point p { color: #94a3b8; }

    .legal { background: rgba(0,0,0,0.3); padding: 1.5rem; border-radius: 12px; margin-bottom: 2rem; }
    .btn-back { color: var(--light); text-decoration: none; font-weight: 600; display: block; text-align: center; }
    .btn-back:hover { color: var(--primary); }
</style>

---
import Layout from '../layouts/Layout.astro';
import { competitorData } from '../data/competitorData';

const title = "GetNexo vs Concorrentes | Comparativo de Plataformas WhatsApp 2026";
const description = "Compare o GetNexo com Twilio, ManyChat, Z-API e Wati. Veja tabela de preços, funcionalidades e descubra a melhor API de WhatsApp.";
const keywords = "comparativo api whatsapp, getnexo vs twilio, getnexo vs manychat, melhor api whatsapp 2026, alternativa twilio brasil";
---

<Layout title={title} description={description} keywords={keywords}>
  
  <section class="comp-hero">
    <div class="container mx-auto px-6 max-w-4xl text-center">
      <div class="hero-badge">Batalha de APIs</div>
      <h1 class="text-gradient mb-6">Comparativo de Plataformas</h1>
      <p class="text-slate-400 text-xl max-w-2xl mx-auto">
        Não contrate antes de comparar. Veja por que mais de 5.000 empresas trocaram suas ferramentas antigas pelo GetNexo.
      </p>
    </div>
  </section>

  <section class="comp-grid-section pb-24">
    <div class="container mx-auto px-6 max-w-6xl">
      
      <div class="grid md:grid-cols-2 lg:grid-cols-2 gap-8">
        {competitorData.map(comp => (
          <a href={`/comparativo/${comp.slug}`} class="comp-card glass-panel group">
            <div class="flex justify-between items-start mb-6">
              <div class="flex items-center gap-4">
                <div class="vs-badge">VS</div>
                <div>
                  <h3 class="text-2xl font-bold text-white group-hover:text-[#00ff9d] transition">GetNexo vs {comp.name}</h3>
                  <span class="text-sm text-slate-500 font-mono">{comp.type}</span>
                </div>
              </div>
              <span class="arrow-icon">→</span>
            </div>
            
            <p class="text-slate-400 mb-6 line-clamp-2">
              {comp.whySwitch}
            </p>

            <div class="verdict-mini">
              <span class="text-xs uppercase font-bold text-[#00ff9d] mb-1 block">Veredito Rápido</span>
              <p class="text-white text-sm font-medium">{comp.verdict}</p>
            </div>
          </a>
        ))}
      </div>

    </div>
  </section>

  <!-- Global Comparison Table -->
  <section class="py-20 bg-[#050912]">
    <div class="container mx-auto px-6 max-w-5xl">
      <h2 class="text-3xl font-bold text-center text-white mb-12">Tabela Geral de Recursos</h2>
      
      <div class="overflow-x-auto rounded-2xl border border-white/10">
        <table class="w-full text-left border-collapse">
          <thead>
            <tr class="bg-white/5">
              <th class="p-6 text-slate-400 font-bold border-b border-white/10">Recurso</th>
              <th class="p-6 text-[#00ff9d] font-black border-b border-white/10 text-xl">GetNexo</th>
              <th class="p-6 text-slate-400 font-bold border-b border-white/10">Outros (Média)</th>
            </tr>
          </thead>
          <tbody class="text-slate-300">
            <tr class="border-b border-white/5 hover:bg-white/5 transition">
              <td class="p-6 font-medium">Custo por Mensagem</td>
              <td class="p-6 text-[#00ff9d] font-bold">R$ 0,00 (Ilimitado)</td>
              <td class="p-6 text-red-400">U$ 0.05 / msg</td>
            </tr>
            <tr class="border-b border-white/5 hover:bg-white/5 transition">
              <td class="p-6 font-medium">PIX Nativo</td>
              <td class="p-6 text-[#00ff9d] font-bold">✅ Sim (Automático)</td>
              <td class="p-6">❌ Não (Depende de 3º)</td>
            </tr>
            <tr class="border-b border-white/5 hover:bg-white/5 transition">
              <td class="p-6 font-medium">IA Generativa (Llama 3)</td>
              <td class="p-6 text-[#00ff9d] font-bold">✅ Incluída</td>
              <td class="p-6">⚠️ Pago à parte</td>
            </tr>
            <tr class="border-b border-white/5 hover:bg-white/5 transition">
              <td class="p-6 font-medium">Interface Visual</td>
              <td class="p-6 text-[#00ff9d] font-bold">✅ Sim (No-Code)</td>
              <td class="p-6">⚠️ Apenas API</td>
            </tr>
            <tr class="hover:bg-white/5 transition">
              <td class="p-6 font-medium">Suporte</td>
              <td class="p-6 text-[#00ff9d] font-bold">🇧🇷 Português</td>
              <td class="p-6">🇺🇸 Inglês</td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </section>

</Layout>

<style>
  .comp-hero { padding: 8rem 0 4rem; }
  .hero-badge { background: rgba(0, 212, 255, 0.1); color: #00d4ff; padding: 0.5rem 1.2rem; border-radius: 50px; font-weight: 700; display: inline-block; margin-bottom: 1.5rem; border: 1px solid rgba(0, 212, 255, 0.2); }
  
  .comp-card { padding: 2.5rem; transition: 0.3s; display: block; text-decoration: none; border: 1px solid rgba(255,255,255,0.05); border-radius: 20px; }
  .comp-card:hover { transform: translateY(-5px); border-color: #00ff9d; background: rgba(255,255,255,0.02); }
  
  .vs-badge { background: #00ff9d; color: black; font-weight: 900; font-size: 0.8rem; padding: 0.3rem 0.6rem; border-radius: 8px; }
  .arrow-icon { font-size: 1.5rem; color: #00d4ff; opacity: 0; transition: 0.3s; transform: translateX(-10px); }
  .group:hover .arrow-icon { opacity: 1; transform: translateX(0); }

  .verdict-mini { background: rgba(0, 255, 157, 0.05); padding: 1rem; border-radius: 12px; border: 1px solid rgba(0, 255, 157, 0.1); }
</style>

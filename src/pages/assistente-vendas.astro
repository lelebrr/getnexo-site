---
import Layout from '../layouts/Layout.astro';
const title = "Assistente de Vendas WhatsApp Grátis – IA que Vende Sozinha 24h sem Mensalidade GetNexo";
const description = "Assistente de vendas WhatsApp com IA que responde, qualifica leads, faz upsell e fecha vendas 24h. 100% grátis, zero nuvem, zero mensalidade, roda no seu PC. Melhor que Suri e BotZapIA.";
const keywords = "assistente vendas whatsapp gratis, ia vende sozinho whatsapp 24h, assistente vendas whatsapp automatico, chatbot vendas whatsapp gratis, automacao vendas whatsapp sem nuvem, getnexo assistente vendas, vendas automaticas whatsapp";
const ogImage = "https://getnexo.com.br/assets/og-poster.jpg";
---
<Layout title={title} description={description} keywords={keywords} ogImage={ogImage}>
  <section class="assistant-hero">
    <div class="hero-badge">IA Conversacional v4.0</div>
    <h1 class="text-gradient">Assistente de Vendas 24/7</h1>
    <p>O cérebro que transforma conversas em faturamento sem precisar de intervenção humana.</p>
  </section>

  <section class="assistant-features">
    <div class="features-grid">
      <div class="feature-card glass-panel">
        <span class="icon">🤖</span>
        <h3>Atendimento Humanizado</h3>
        <p>Utilizamos modelos LLM avançados que entendem gírias, contextos e objeções do consumidor brasileiro.</p>
      </div>
      <div class="feature-card glass-panel">
        <span class="icon">🎯</span>
        <h3>Qualificação em Tempo Real</h3>
        <p>O assistente identifica o potencial do lead e já o segmenta no CRM antes mesmo de você abrir o chat.</p>
      </div>
      <div class="feature-card glass-panel">
        <span class="icon">📦</span>
        <h3>Checkout Direto no Chat</h3>
        <p>Integração total com gateways de pagamento. O bot gera o link de PIX ou Cartão em segundos.</p>
      </div>
    </div>
  </section>

  <section class="assistant-comparison">
    <h2 class="section-title">Por que o modelo GetNexo é imbatível?</h2>
    <div class="comparison-container glass-panel">
      <div class="comp-row head">
        <div>Recurso</div>
        <div>SaaS Comum</div>
        <div class="highlight">GetNexo AI</div>
      </div>
      <div class="comp-row">
        <div>Custo Mensal</div>
        <div>R$ 399 - R$ 1.200</div>
        <div class="highlight">R$ 0,00</div>
      </div>
      <div class="comp-row">
        <div>Privacidade</div>
        <div>Nuvem Terceirizada</div>
        <div class="highlight">Seu Servidor Local</div>
      </div>
      <div class="comp-row">
        <div>IA Própria</div>
        <div>Limitada / Rígida</div>
        <div class="highlight">Customização Total</div>
      </div>
    </div>
  </section>

  <section class="assistant-cta">
    <div class="glass-panel cta-card">
        <h2>Pare de perder vendas por demora</h2>
        <p>Ative seu assistente agora e comece a vender enquanto dorme.</p>
        <div class="cta-actions">
            <a href="#" onclick="window.$chatwoot.toggle(); return false;" class="btn-primary-glow">Testar Assistente IA</a>
            <a href="/como-funciona" class="btn-outline">Ver Fluxo Técnico</a>
        </div>
    </div>
  </section>

<style>
  .assistant-hero { text-align: center; padding: 10rem 5% 4rem; }
  .hero-badge { background: rgba(0, 212, 255, 0.1); color: #00d4ff; padding: 0.5rem 1.2rem; border-radius: 50px; font-size: 0.85rem; font-weight: 700; display: inline-block; margin-bottom: 2rem; border: 1px solid rgba(0,212,255,0.2); }
  .assistant-hero h1 { font-size: 3.5rem; margin-bottom: 1rem; }
  .assistant-hero p { color: #94a3b8; font-size: 1.25rem; max-width: 600px; margin: 0 auto; }

  .assistant-features { padding: 4rem 5%; max-width: 1200px; margin: 0 auto; }
  .features-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 2rem; }
  .feature-card { padding: 3rem; text-align: center; border-radius: 24px; border: 1px solid rgba(255,255,255,0.05); }
  .feature-card .icon { font-size: 3.5rem; display: block; margin-bottom: 1.5rem; }
  .feature-card h3 { color: white; margin-bottom: 1rem; font-size: 1.5rem; font-weight: 800; }
  .feature-card p { color: #94a3b8; line-height: 1.7; font-size: 1.1rem; }

  .assistant-comparison { padding: 6rem 5%; max-width: 900px; margin: 0 auto; }
  .section-title { text-align: center; font-size: 2.5rem; margin-bottom: 3rem; color: white; font-weight: 800; }
  .comparison-container { border-radius: 24px; overflow: hidden; border: 1px solid rgba(255,255,255,0.05); }
  .comp-row { display: grid; grid-template-columns: 1fr 1fr 1fr; border-bottom: 1px solid rgba(255,255,255,0.05); }
  .comp-row div { padding: 1.5rem 2rem; color: #94a3b8; font-weight: 600; }
  .comp-row.head { background: rgba(255,255,255,0.02); }
  .comp-row.head div { color: white; font-weight: 800; }
  .comp-row div.highlight { color: #00ff9d; background: rgba(0, 255, 157, 0.05); }

  .assistant-cta { padding: 6rem 5% 10rem; max-width: 900px; margin: 0 auto; }
  .cta-card { padding: 4rem; text-align: center; border-radius: 32px; }
  .cta-card h2 { color: white; font-size: 2.5rem; margin-bottom: 1rem; font-weight: 800; }
  .cta-card p { color: #94a3b8; margin-bottom: 2.5rem; font-size: 1.1rem; }
  .cta-actions { display: flex; justify-content: center; gap: 1.5rem; }

  .btn-primary-glow { background: linear-gradient(135deg, #00d4ff, #00ff9d); color: black; padding: 1.1rem 2.5rem; border-radius: 12px; font-weight: 900; text-decoration: none; transition: 0.3s; box-shadow: 0 10px 30px rgba(0, 212, 255, 0.2); }
  .btn-primary-glow:hover { transform: scale(1.05); box-shadow: 0 15px 40px rgba(0, 212, 255, 0.4); }
  .btn-outline { border: 1px solid rgba(255,255,255,0.1); color: white; padding: 1.1rem 2.5rem; border-radius: 12px; font-weight: 700; text-decoration: none; transition: 0.3s; background: rgba(255,255,255,0.03); }

  @media (max-width: 768px) {
    .assistant-hero h1 { font-size: 2.8rem; }
    .comp-row { grid-template-columns: 1fr; }
    .comp-row div { padding: 1rem; border-bottom: 1px solid rgba(255,255,255,0.05); }
    .cta-actions { flex-direction: column; }
    .cta-card { padding: 2rem; }
  }
</style>
</Layout>

<style>
  .hero {
    text-align: center;
    padding: 100px 5% 80px;
    background: linear-gradient(135deg, var(--dark), #0a1128);
    position: relative;
  }
  .hero::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0; bottom: 0;
    background: radial-gradient(ellipse at center, rgba(0,212,255,0.15) 0%, transparent 70%);
    pointer-events: none;
  }
  .hero h1 {
    font-size: clamp(2.2rem, 6vw, 3.8rem);
    background: linear-gradient(90deg, var(--primary), var(--success));
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    margin: 0 0 1.5rem;
    line-height: 1.2;
  }
  .hero h1 strong { color: var(--success); -webkit-text-fill-color: var(--success); }
  .subtitle {
    font-size: clamp(1rem, 2.5vw, 1.4rem);
    color: var(--text);
    max-width: 800px;
    margin: 0 auto 2rem;
  }
  .hero-badges {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 2.5rem;
  }
  .hero-badges span {
    background: rgba(0,212,255,0.1);
    border: 1px solid rgba(0,212,255,0.3);
    padding: 0.5rem 1.2rem;
    border-radius: 50px;
    font-size: 0.9rem;
    color: var(--light);
  }
  .buttons { display: flex; gap: 1.5rem; justify-content: center; flex-wrap: wrap; }
  .btn-primary {
    display: inline-block;
    background: linear-gradient(90deg, var(--success), #00cc88);
    color: black;
    padding: 1rem 2.5rem;
    border-radius: 50px;
    font-weight: 800;
    text-decoration: none;
    transition: all 0.3s;
    box-shadow: 0 5px 20px rgba(0,255,157,0.3);
  }
  .btn-primary:hover {
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 10px 30px rgba(0,255,157,0.5);
  }
  .btn-secondary {
    display: inline-block;
    color: var(--light);
    border: 2px solid var(--primary);
    padding: 1rem 2rem;
    border-radius: 50px;
    font-weight: 700;
    text-decoration: none;
    transition: all 0.3s;
  }
  .btn-secondary:hover { background: var(--primary); color: black; }

  .features {
    padding: 80px 5%;
    max-width: 1200px;
    margin: 0 auto;
  }
  .features h2 {
    text-align: center;
    font-size: clamp(1.8rem, 4vw, 2.5rem);
    margin: 0 0 3rem;
    background: linear-gradient(90deg, var(--primary), var(--success));
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }
  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
  }
  .feature-card {
    background: rgba(30,41,59,0.7);
    border: 1px solid rgba(0,212,255,0.15);
    border-radius: 16px;
    padding: 2rem;
    text-align: center;
    transition: all 0.4s;
  }
  .feature-card:hover {
    transform: translateY(-8px);
    border-color: var(--primary);
    box-shadow: 0 15px 30px rgba(0,212,255,0.2);
  }
  .feature-card .icon { font-size: 2.5rem; display: block; margin-bottom: 1rem; }
  .feature-card h3 { color: var(--success); margin: 0 0 0.8rem; font-size: 1.3rem; }
  .feature-card p { color: var(--text); margin: 0; line-height: 1.6; }

  .comparison {
    padding: 80px 5%;
    background: rgba(30,41,59,0.5);
    text-align: center;
  }
  .comparison h2 {
    font-size: clamp(1.8rem, 4vw, 2.5rem);
    margin: 0 0 3rem;
    color: var(--light);
  }
  .comparison-bars {
    max-width: 600px;
    margin: 0 auto 2rem;
  }
  .bar {
    display: flex;
    align-items: center;
    margin-bottom: 1rem;
    position: relative;
    height: 50px;
    background: rgba(10,14,23,0.8);
    border-radius: 8px;
    overflow: hidden;
  }
  .bar .label {
    position: absolute;
    left: 1rem;
    z-index: 2;
    font-weight: 600;
    color: var(--light);
  }
  .bar .value {
    position: absolute;
    right: 1rem;
    z-index: 2;
    font-weight: 800;
  }
  .bar-fill {
    height: 100%;
    border-radius: 8px;
    position: absolute;
    left: 0;
  }
  .bar.getnexo .bar-fill { background: linear-gradient(90deg, var(--success), #00cc88); }
  .bar.getnexo .value { color: var(--success); }
  .bar.competitor .bar-fill { background: linear-gradient(90deg, #ff4d4d, #ff6b6b); }
  .bar.competitor .value { color: #ff6b6b; }

  .cta-final {
    text-align: center;
    padding: 80px 5%;
    background: linear-gradient(135deg, rgba(0,212,255,0.1), rgba(0,255,157,0.1));
  }
  .cta-final h2 {
    font-size: clamp(1.8rem, 4vw, 2.5rem);
    color: var(--light);
    margin: 0 0 1rem;
  }
  .cta-final p {
    color: var(--text);
    font-size: 1.2rem;
    margin: 0 0 2rem;
  }

  @media (max-width: 768px) {
    .hero { padding: 80px 5% 60px; }
    .hero-badges span { font-size: 0.8rem; padding: 0.4rem 0.8rem; }
    .features-grid { grid-template-columns: 1fr; }
  }
</style>

---
import Layout from '../layouts/Layout.astro';
---
<Layout title="Criar Bot GetNexo – Instale em qualquer site">

  <div class="wizard-container">
    <header class="wizard-header">
      <h1>Criar Bot GetNexo</h1>
      <p>Configure seu assistente de vendas em 3 passos simples</p>
    </header>

    <!-- Step Indicators -->
    <div class="step-indicators">
      <div class="step-item active" data-step="1">
        <span class="step-number">1</span>
        <span class="step-label">Negócio</span>
      </div>
      <div class="step-line"></div>
      <div class="step-item" data-step="2">
        <span class="step-number">2</span>
        <span class="step-label">Loja</span>
      </div>
      <div class="step-line"></div>
      <div class="step-item" data-step="3">
        <span class="step-number">3</span>
        <span class="step-label">Pronto</span>
      </div>
    </div>

    <!-- Step 1: Business Info -->
    <div class="step-content" id="step1">
      <h2>📋 Informações do Negócio</h2>
      <form id="form1" class="wizard-form">
        <div class="form-grid">
          <div class="field">
            <label>Nome do Bot / Loja *</label>
            <input type="text" name="nome" placeholder="Ex: Loja da Maria" required />
          </div>
          <div class="field">
            <label>WhatsApp *</label>
            <input type="tel" name="whatsapp" placeholder="(11) 99999-9999" required />
          </div>
          <div class="field">
            <label>E-mail Comercial *</label>
            <input type="email" name="email" placeholder="contato@sualoja.com.br" required />
          </div>
          <div class="field">
            <label>Segmento *</label>
            <select name="segmento" required>
              <option value="">Selecione...</option>
              <option value="moda">Moda & Vestuário</option>
              <option value="eletronicos">Eletrônicos</option>
              <option value="cosmeticos">Cosméticos</option>
              <option value="alimentacao">Alimentação</option>
              <option value="servicos">Serviços</option>
              <option value="outro">Outro</option>
            </select>
          </div>
        </div>
        <button type="submit" class="btn-next">Próximo →</button>
      </form>
    </div>

    <!-- Step 2: E-commerce Integration -->
    <div class="step-content hidden" id="step2">
      <h2>🛒 Conectar à Loja</h2>
      <p class="step-desc">Escolha sua plataforma de e-commerce (opcional):</p>
      
      <div class="platforms-grid">
        <label class="platform-card selected">
          <input type="radio" name="plataforma" value="nenhuma" checked />
          <div class="platform-icon">🚫</div>
          <span class="platform-name">Sem integração</span>
        </label>
        
        <label class="platform-card">
          <input type="radio" name="plataforma" value="shopify" />
          <img src="https://cdn.shopify.com/s/files/1/0558/6938/1056/files/shopify-icon.svg" alt="Shopify" class="platform-logo" onerror="this.outerHTML='<div class=\'platform-icon\'>🟢</div>'" />
          <span class="platform-name">Shopify</span>
        </label>
        
        <label class="platform-card">
          <input type="radio" name="plataforma" value="woocommerce" />
          <img src="https://woocommerce.com/wp-content/themes/flavor-flavor/assets/images/woo-logo.svg" alt="WooCommerce" class="platform-logo" onerror="this.outerHTML='<div class=\'platform-icon\'>🟣</div>'" />
          <span class="platform-name">WooCommerce</span>
        </label>
        
        <label class="platform-card">
          <input type="radio" name="plataforma" value="vtex" />
          <div class="platform-icon" style="color:#f71963;">V</div>
          <span class="platform-name">VTEX</span>
        </label>
        
        <label class="platform-card">
          <input type="radio" name="plataforma" value="nuvemshop" />
          <div class="platform-icon">☁️</div>
          <span class="platform-name">Nuvemshop</span>
        </label>
        
        <label class="platform-card">
          <input type="radio" name="plataforma" value="mercadolivre" />
          <div class="platform-icon" style="color:#ffe600;">M</div>
          <span class="platform-name">Mercado Livre</span>
        </label>
        
        <label class="platform-card">
          <input type="radio" name="plataforma" value="tray" />
          <div class="platform-icon" style="color:#00a8e8;">T</div>
          <span class="platform-name">Tray</span>
        </label>
        
        <label class="platform-card">
          <input type="radio" name="plataforma" value="lojaintegrada" />
          <div class="platform-icon" style="color:#ff6600;">L</div>
          <span class="platform-name">Loja Integrada</span>
        </label>
      </div>
      
      <div class="step-actions">
        <button type="button" class="btn-back" onclick="goToStep(1)">← Voltar</button>
        <button type="button" class="btn-next" onclick="goToStep(3)">Gerar Código →</button>
      </div>
    </div>

    <!-- Step 3: Widget Code -->
    <div class="step-content hidden" id="step3">
      <h2>✅ Pronto!</h2>
      
      <div class="success-box">
        <div class="success-icon">🎉</div>
        <h3>Seu bot foi criado!</h3>
        <p>Cole o código abaixo antes do <code>&lt;/body&gt;</code> no seu site:</p>
      </div>
      
      <div class="code-box">
        <code id="widget-code">&lt;script src="https://getnexo.com.br/widget.js" data-bot="..."&gt;&lt;/script&gt;</code>
        <button onclick="copiarCodigo()" class="btn-copy">📋 Copiar</button>
      </div>
      
      <div class="next-steps">
        <h4>Próximos passos:</h4>
        <ul>
          <li>✅ Cole o código no seu site</li>
          <li>✅ Acesse o <a href="/admin">Painel Admin</a> para configurar</li>
          <li>✅ Teste o chat no seu site</li>
        </ul>
      </div>
      
      <div class="step-actions">
        <button type="button" class="btn-back" onclick="goToStep(2)">← Voltar</button>
        <a href="/admin" class="btn-next">Ir para o Admin →</a>
      </div>
    </div>
  </div>

  <script is:inline>
    let botData = {};
    let currentStep = 1;
    
    function goToStep(step) {
      // Update step indicators
      document.querySelectorAll('.step-item').forEach((el, i) => {
        if (i + 1 <= step) {
          el.classList.add('active');
        } else {
          el.classList.remove('active');
        }
      });
      
      // Show/hide content
      document.querySelectorAll('.step-content').forEach((el, i) => {
        if (i + 1 === step) {
          el.classList.remove('hidden');
        } else {
          el.classList.add('hidden');
        }
      });
      
      currentStep = step;
      
      // If step 3, generate code
      if (step === 3) {
        generateCode();
      }
    }
    
    // Form 1 Submit
    document.getElementById('form1').onsubmit = (e) => {
      e.preventDefault();
      const form = new FormData(e.target);
      botData = {
        nome: form.get('nome'),
        whatsapp: form.get('whatsapp'),
        email: form.get('email'),
        segmento: form.get('segmento')
      };
      goToStep(2);
    };
    
    // Platform selection styling
    document.querySelectorAll('.platform-card').forEach(card => {
      card.addEventListener('click', () => {
        document.querySelectorAll('.platform-card').forEach(c => c.classList.remove('selected'));
        card.classList.add('selected');
      });
    });
    
    function generateCode() {
      const plataforma = document.querySelector('input[name="plataforma"]:checked').value;
      botData.plataforma = plataforma;
      botData.id = 'bot_' + Date.now();
      
      const scriptUrl = window.location.hostname === 'localhost' 
        ? 'http://localhost:8080/widget.js' 
        : 'https://getnexo.com.br/widget.js';
      
      document.getElementById('widget-code').textContent = 
        `<script src="${scriptUrl}" data-bot="${botData.id}" data-theme="neon"><\/script>`;
    }
    
    window.copiarCodigo = function() {
      const code = document.getElementById('widget-code').textContent;
      navigator.clipboard.writeText(code).then(() => {
        alert('✅ Código copiado!');
      });
    };
  </script>
</Layout>

<style>
  .wizard-container {
    max-width: 700px;
    margin: 0 auto;
    padding: 3rem 2rem;
    min-height: 100vh;
  }
  
  .wizard-header {
    text-align: center;
    margin-bottom: 3rem;
  }
  
  .wizard-header h1 {
    color: #00ff9d;
    font-size: 2.5rem;
    margin: 0;
  }
  
  .wizard-header p {
    color: #94a3b8;
    margin-top: 0.5rem;
  }
  
  /* Step Indicators */
  .step-indicators {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 3rem;
  }
  
  .step-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    opacity: 0.4;
    transition: opacity 0.3s;
  }
  
  .step-item.active {
    opacity: 1;
  }
  
  .step-number {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: #1e293b;
    border: 2px solid #334155;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: bold;
  }
  
  .step-item.active .step-number {
    background: #00ff9d;
    border-color: #00ff9d;
    color: black;
  }
  
  .step-label {
    font-size: 0.8rem;
    color: #94a3b8;
  }
  
  .step-line {
    width: 60px;
    height: 2px;
    background: #334155;
  }
  
  /* Step Content */
  .step-content {
    background: rgba(20,30,50,0.6);
    border-radius: 16px;
    padding: 2rem;
    border: 1px solid #1e293b;
  }
  
  .step-content.hidden {
    display: none;
  }
  
  .step-content h2 {
    color: #00d4ff;
    margin: 0 0 1.5rem 0;
    font-size: 1.4rem;
  }
  
  .step-desc {
    color: #94a3b8;
    margin-bottom: 1.5rem;
  }
  
  /* Form */
  .form-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
  }
  
  @media (max-width: 600px) {
    .form-grid {
      grid-template-columns: 1fr;
    }
  }
  
  .field {
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
  }
  
  .field label {
    color: #94a3b8;
    font-size: 0.85rem;
  }
  
  .field input, .field select {
    padding: 0.8rem;
    border-radius: 8px;
    background: #0f172a;
    color: white;
    border: 1px solid #334155;
    font-size: 1rem;
  }
  
  .field input:focus, .field select:focus {
    outline: none;
    border-color: #00d4ff;
  }
  
  /* Buttons */
  .btn-next {
    background: linear-gradient(90deg, #00d4ff, #00ff9d);
    color: black;
    padding: 1rem 2rem;
    border: none;
    border-radius: 50px;
    font-weight: 700;
    cursor: pointer;
    margin-top: 1.5rem;
    font-size: 1rem;
    display: inline-block;
    text-decoration: none;
  }
  
  .btn-back {
    background: transparent;
    color: #94a3b8;
    padding: 1rem 2rem;
    border: 1px solid #334155;
    border-radius: 50px;
    cursor: pointer;
    font-size: 1rem;
  }
  
  .step-actions {
    display: flex;
    justify-content: space-between;
    margin-top: 2rem;
  }
  
  /* Platforms Grid */
  .platforms-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0.8rem;
  }
  
  @media (max-width: 600px) {
    .platforms-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  
  .platform-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 1rem;
    background: #0f172a;
    border: 2px solid #1e293b;
    border-radius: 12px;
    cursor: pointer;
    transition: all 0.2s;
    gap: 0.5rem;
  }
  
  .platform-card:hover {
    border-color: #334155;
  }
  
  .platform-card.selected {
    border-color: #00ff9d;
    background: rgba(0,255,157,0.1);
  }
  
  .platform-card input {
    display: none;
  }
  
  .platform-icon {
    font-size: 2rem;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
  }
  
  .platform-logo {
    width: 40px;
    height: 40px;
    object-fit: contain;
  }
  
  .platform-name {
    color: #e5e7eb;
    font-size: 0.8rem;
    text-align: center;
  }
  
  /* Success Box */
  .success-box {
    text-align: center;
    padding: 2rem;
    background: rgba(0,255,157,0.1);
    border-radius: 12px;
    margin-bottom: 1.5rem;
  }
  
  .success-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }
  
  .success-box h3 {
    color: #00ff9d;
    margin: 0;
  }
  
  .success-box p {
    color: #94a3b8;
    margin-top: 0.5rem;
  }
  
  /* Code Box */
  .code-box {
    background: #0f172a;
    border-radius: 8px;
    padding: 1rem;
    display: flex;
    align-items: center;
    gap: 1rem;
    border: 1px solid #00ff9d55;
  }
  
  .code-box code {
    flex: 1;
    color: #00ff9d;
    font-size: 0.85rem;
    word-break: break-all;
  }
  
  .btn-copy {
    background: #00ff9d;
    color: black;
    border: none;
    padding: 0.5rem 1rem;
    border-radius: 6px;
    cursor: pointer;
    font-weight: 600;
    white-space: nowrap;
  }
  
  /* Next Steps */
  .next-steps {
    margin-top: 1.5rem;
    padding: 1rem;
    background: #1e293b;
    border-radius: 8px;
  }
  
  .next-steps h4 {
    color: #00d4ff;
    margin: 0 0 0.5rem 0;
    font-size: 0.9rem;
  }
  
  .next-steps ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  
  .next-steps li {
    color: #94a3b8;
    padding: 0.3rem 0;
    font-size: 0.9rem;
  }
  
  .next-steps a {
    color: #00d4ff;
  }
</style>

---
import Layout from '../layouts/Layout.astro';
const title = "Como Funciona o GetNexo – Automação WhatsApp IA Sem Mensalidades";
const description = "Entenda como o GetNexo transforma seu PC em uma máquina de vendas automática via WhatsApp usando Evolution API, n8n e IA Generativa local.";
---

<Layout title={title} description={description}>
  <section class="how-hero">
    <div class="hero-badge">Simples, Rápido e Poderoso</div>
    <h1 class="text-gradient">Automação Sem Segredos</h1>
    <p>Do zero ao primeiro bot em menos de 20 minutos. Sem mensalidades, sem taxas oculta e com controle total dos seus dados.</p>
  </section>

  <section class="how-steps">
    <div class="steps-grid">
      <div class="step-card glass-panel">
        <div class="step-icon">💻</div>
        <div class="step-info">
          <span class="step-num">ETAPA 01</span>
          <h3>Infraestrutura Local</h3>
          <p>Instalação via Docker no seu servidor ou PC. Você tem controle total dos dados e <strong>zero latência</strong> de nuvem.</p>
          <div class="tech-stack">
            <span>Docker</span> <span>WSL2</span> <span>Linux</span>
          </div>
        </div>
      </div>
      
      <div class="step-card glass-panel">
        <div class="step-icon">🔗</div>
        <div class="step-info">
          <span class="step-num">ETAPA 02</span>
          <h3>Conexão Oficial</h3>
          <p>Conecte o WhatsApp via QR Code em segundos usando a Evolution API. Estável, seguro e aprovado pela Meta.</p>
          <div class="tech-stack">
            <span>Evolution API</span> <span>Oficial</span>
          </div>
        </div>
      </div>
      
      <div class="step-card glass-panel">
        <div class="step-icon">🧠</div>
        <div class="step-info">
          <span class="step-num">ETAPA 03</span>
          <h3>IA Generativa</h3>
          <p>Alimente a IA com seu conhecimento de negócio. Ela passa a agir como seu melhor vendedor, 24 horas por dia.</p>
          <div class="tech-stack">
            <span>Llama 3</span> <span>GPT-4o</span> <span>Gemini</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="how-flow">
    <div class="glass-panel flow-card">
        <h2>O Ciclo da Venda Automática</h2>
        <div class="flow-visual">
          <div class="f-node">
            <div class="f-icon">💬</div>
            <h4>Atração</h4>
            <p>Cliente inicia chat</p>
          </div>
          <div class="f-arrow">→</div>
          <div class="f-node">
            <div class="f-icon">🤖</div>
            <h4>Interação</h4>
            <p>IA qualifica lead</p>
          </div>
          <div class="f-arrow">→</div>
          <div class="f-node">
            <div class="f-icon">🛒</div>
            <h4>Conversão</h4>
            <p>Venda via PIX</p>
          </div>
          <div class="f-arrow">→</div>
          <div class="f-node">
            <div class="f-icon">💰</div>
            <h4>Sucesso</h4>
            <p>Lucro na conta</p>
          </div>
        </div>
    </div>
  </section>

  <section class="how-specs">
    <h2>Requisitos de Sistema</h2>
    <div class="specs-grid">
      <div class="spec-card glass-panel">
        <h4>Hardware</h4>
        <ul>
          <li>8GB RAM (mínimo)</li>
          <li>SSD 20GB livre</li>
          <li>CPU i3 ou superior</li>
        </ul>
      </div>
      <div class="spec-card glass-panel">
        <h4>Software</h4>
        <ul>
          <li>Windows 10/11 (WSL)</li>
          <li>Ubuntu 22.04+</li>
          <li>Docker Desktop</li>
        </ul>
      </div>
      <div class="spec-card glass-panel">
        <h4>Rede</h4>
        <ul>
          <li>Internet Fibra</li>
          <li>Cloudflare Tunnel</li>
          <li>DNS Configurado</li>
        </ul>
      </div>
    </div>
  </section>

  <section class="how-cta">
    <div class="glass-panel final-cta">
        <h2>Pronto para a nova era?</h2>
        <p>A automação que você sempre quis, com a liberdade que você merece.</p>
        <div class="actions">
            <a href="/criar-bot" class="btn-primary-glow">Criar Meu Bot Agora</a>
            <a href="/documentacao" class="btn-outline">Ver Documentação</a>
        </div>
    </div>
  </section>
</Layout>

<style>
  .how-hero { text-align: center; padding: 10rem 5% 5rem; }
  .hero-badge { background: rgba(0, 212, 255, 0.1); color: #00d4ff; padding: 0.5rem 1.2rem; border-radius: 50px; font-size: 0.85rem; font-weight: 700; display: inline-block; margin-bottom: 2rem; border: 1px solid rgba(0,212,255,0.2); }
  .how-hero h1 { font-size: 3.5rem; margin-bottom: 1.5rem; font-weight: 800; }
  .how-hero p { color: #94a3b8; font-size: 1.25rem; max-width: 700px; margin: 0 auto; }

  .how-steps { padding: 4rem 5%; max-width: 1200px; margin: 0 auto; }
  .steps-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 2.5rem; }
  .step-card { padding: 3rem; display: flex; gap: 2rem; align-items: flex-start; transition: 0.3s; }
  .step-card:hover { transform: translateY(-10px); border-color: #00ff9d; }
  .step-icon { font-size: 3rem; background: rgba(255,255,255,0.03); width: 80px; height: 80px; display: flex; align-items: center; justify-content: center; border-radius: 20px; flex-shrink: 0; }
  .step-num { color: #00ff9d; font-weight: 900; font-size: 0.8rem; letter-spacing: 2px; margin-bottom: 0.5rem; display: block; }
  .step-info h3 { color: white; margin-bottom: 1rem; font-size: 1.6rem; font-weight: 800; }
  .step-info p { color: #94a3b8; line-height: 1.7; }
  .tech-stack { display: flex; gap: 0.8rem; margin-top: 1.5rem; }
  .tech-stack span { font-size: 0.75rem; background: rgba(255,255,255,0.05); padding: 0.4rem 0.8rem; border-radius: 6px; color: #64748b; font-weight: 600; }

  .how-flow { padding: 6rem 5%; max-width: 1200px; margin: 0 auto; }
  .flow-card { padding: 5rem; text-align: center; border-radius: 40px; }
  .flow-card h2 { color: white; font-size: 2.5rem; margin-bottom: 4rem; font-weight: 800; }
  .flow-visual { display: flex; justify-content: space-between; align-items: center; gap: 1rem; }
  .f-node { flex: 1; }
  .f-icon { font-size: 3rem; margin-bottom: 1rem; }
  .f-node h4 { color: #00ff9d; font-size: 1.2rem; margin-bottom: 0.5rem; }
  .f-node p { color: #64748b; font-size: 0.9rem; }
  .f-arrow { font-size: 2rem; color: #1e293b; font-weight: 900; }

  .how-specs { padding: 6rem 5%; text-align: center; max-width: 1200px; margin: 0 auto; }
  .how-specs h2 { color: white; font-size: 2.2rem; margin-bottom: 3rem; font-weight: 800; }
  .specs-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 2rem; text-align: left; }
  .spec-card { padding: 2.5rem; border-radius: 24px; }
  .spec-card h4 { color: #00d4ff; margin-bottom: 1.5rem; font-size: 1.2rem; padding-bottom: 1rem; border-bottom: 1px solid rgba(255,255,255,0.05); }
  .spec-card ul { list-style: none; padding: 0; }
  .spec-card li { color: #94a3b8; padding: 0.8rem 0; border-bottom: 1px dotted rgba(255,255,255,0.05); }
  .spec-card li:last-child { border-bottom: none; }

  .how-cta { padding: 6rem 5% 10rem; max-width: 1000px; margin: 0 auto; }
  .final-cta { padding: 5rem; text-align: center; border-radius: 40px; border: 1px solid rgba(0, 212, 255, 0.1); }
  .final-cta h2 { color: white; font-size: 2.8rem; margin-bottom: 1rem; font-weight: 800; }
  .final-cta p { color: #94a3b8; margin-bottom: 3rem; font-size: 1.2rem; }
  .actions { display: flex; justify-content: center; gap: 2rem; }

  .btn-primary-glow { background: linear-gradient(135deg, #00d4ff, #00ff9d); color: black; padding: 1.2rem 3rem; border-radius: 12px; font-weight: 900; text-decoration: none; transition: 0.3s; box-shadow: 0 10px 30px rgba(0,212,255,0.2); }
  .btn-outline { border: 1px solid rgba(255,255,255,0.1); color: white; padding: 1.2rem 3rem; border-radius: 12px; font-weight: 700; text-decoration: none; transition: 0.3s; background: rgba(255,255,255,0.03); }

  @media (max-width: 900px) {
    .flow-visual { flex-direction: column; gap: 3rem; }
    .f-arrow { transform: rotate(90deg); }
    .actions { flex-direction: column; gap: 1rem; }
    .step-card { flex-direction: column; text-align: center; }
    .step-icon { margin: 0 auto; }
    .how-hero h1 { font-size: 2.5rem; }
  }
</style>
